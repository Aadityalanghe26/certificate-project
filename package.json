{
  "name": "certificate-project",
  "version": "1.0.0",
  "description": "CertChain - Blockchain Certificate Platform",
  "main": "index.js",
  "scripts": {
    "node": "hardhat node",
    "deploy": "node scripts/deploy.js",
    "deploy:sepolia": "node scripts/deploy.js --sepolia",
    "compile": "hardhat compile",
    "backend": "node backend/server.js",
    "test": "node --test test/**/*.test.js",
    "test:watch": "node --test --watch test/**/*.test.js"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "certificate",
    "dapp",
    "solidity",
    "ipfs"
  ],
  "author": "CertChain Team",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.4",
    "hardhat": "^3.3.0",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "viem": "^2.48.4"
  }
}
